#pragma once

#include "OrientedGraph.h"

#include <sstream>
#include <string>

template <typename TKey, typename TValue>
void RenderGraph(const OGraph<TKey, TValue>& graph){
    std::string command = "";
}
